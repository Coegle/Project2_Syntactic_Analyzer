# 删除了对可变参数省略号的支持 ... 删除其关键字 ...
# 删除了对指针的支持
    pointer
# 删除了对枚举类型的支持 
    enum_spec
# 删除了对 extern register volatile sizeof signed unsigned typedef static short 关键字的支持
# 删除了对 struct union 类型的支持，同时删除其关键字struct union 以及 . -> 运算符
    struct_or_union_spec
    struct_or_union
    struct_decl
    struct_decl_list
    struct_declarator
    struct_declarator_list
# 删除了对多个 for 循环空语句的支持
# 删除对 goto 的支持
# 删除对移位运算的支持 << >> <<= >>= 支持
# 删除对三目运算符的支持 删除关键字 ? :
# 删除对逻辑运算的支持 ~ & | &= |= ^= % %= ^
# 删除对 switch do while 语句的支持 删除关键字 do switch default case


// 删除 auto const /= *= += -= -> .